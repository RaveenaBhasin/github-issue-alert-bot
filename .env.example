# GitHub Configuration (optional for public repos, recommended for higher rate limits)
# Leave empty to use unauthenticated requests (60/hour limit)
# Set token for authenticated requests (5,000/hour limit) or private repos
GITHUB_TOKEN=

# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Monitoring Configuration
# Multiple repositories: comma-separated (e.g., owner1/repo1,owner2/repo2)
# Or use REPO_NAME for single repo (backward compatible)
REPO_NAMES=paradigmxyz/reth,owner2/repo2
# REPO_NAME=paradigmxyz/reth  # Alternative: single repo (backward compatible)

# Optional: Filter by author (leave empty to monitor all authors)
AUTHOR_NAME=mattsse

# Poll interval in seconds (default: 900 = 15 minutes)
POLL_INTERVAL=900
